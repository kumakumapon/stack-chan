# MiniStack Phase 0

Added a private MiniStack MOD and browser Local Peer test client with bounded command scheduling, request deduplication, conservative motion limits and a heartbeat timeout. Local Peer messages now expose per-message authentication independently of historical peer state. Hardware validation is pending; recording, camera transfer and the MiniStack PC/MCP integration remain future work. See firmware/mods/ministack/README_ja.md.

---
"stack-chan": minor
"stackchan-web": minor
---

Play Gateway PCM replies incrementally with a bounded queue and explicit response interruption from the face tap or Web conversation panel. Cancel synthesis and backend work, discard stale output, and resume half-duplex microphone capture after acknowledgement.

Close every listen-based HTTP response connection to avoid the SDK keep-alive VM failure; preserve custom response statuses. Robot directives remain reserved; embodiment uses the existing tool and approval flow.
